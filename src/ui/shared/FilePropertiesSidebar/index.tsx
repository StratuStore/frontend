import { observer } from "mobx-react-lite"
import { createPortal } from "react-dom"

function FilePropertiesSidebarComponent() {
    const parentRef = document.getElementById("root")

    return null
}

const FilePropertiesSidebar = observer(FilePropertiesSidebarComponent)
export default FilePropertiesSidebar

